# Task Completion Checklist

1. Build passes: `dotnet build src/AutexisCase.API/AutexisCase.API.csproj`
2. Tests pass: `dotnet test src/AutexisCase.Tests/AutexisCase.Tests.csproj`
3. No warnings or errors in build output
4. Code follows conventions from code_style_and_conventions memory
5. Changes are on a feature branch, not main
6. Commit messages are clean, no AI attribution
7. PR created with past-tense title and `resolve #<issue-number>` in body
